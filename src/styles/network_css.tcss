#network-container {
    background: $panel;
    border: lightgreen;
    border-title-color: $text;
    border-title-align: center;
}

#network-screen-static {
    background: $panel;
    content-align: center middle;
    padding: 2 0;
}
