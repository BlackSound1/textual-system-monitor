#network-container {
    background: $panel;
    border: lightgreen;
    border-title-color: $text;
    border-title-align: center;
}

#network-container:light {
    border: green;
    background: $boost-darken-1;
}
